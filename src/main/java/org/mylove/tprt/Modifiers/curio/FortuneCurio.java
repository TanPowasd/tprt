package org.mylove.tprt.Modifiers.curio;
/*
import com.ssakura49.tinkercuriolib.hook.behavior.CurioFortuneModifierHook;
import com.ssakura49.tinkercuriolib.hook.combat.CurioLootingModifierHook;
import net.minecraft.world.damagesource.DamageSource;
import net.minecraft.world.entity.LivingEntity;
import net.minecraft.world.item.ItemStack;
import net.minecraft.world.level.storage.loot.LootContext;
import org.mylove.tprt.common.modifier.modifierModule.CuriosModifier;
import slimeknights.tconstruct.library.modifiers.ModifierEntry;
import slimeknights.tconstruct.library.tools.nbt.IToolStackView;
import top.theillusivec4.curios.api.SlotContext;


/// 提供词条等级的 时运 & 抢夺
public class Fortune extends CuriosModifier{

    @Override
    public int onCurioGetFortune(IToolStackView curio, ModifierEntry entry, SlotContext slotContext, LootContext lootContext, ItemStack stack, int fortune) {
        return CurioFortuneModifierHook.super.onCurioGetFortune(curio, entry, slotContext, lootContext, stack, fortune);
    }

    @Override
    public int onCurioGetLooting(IToolStackView curio, ModifierEntry entry, SlotContext slotContext, DamageSource source, LivingEntity target, ItemStack stack, int looting) {
        return CurioLootingModifierHook.super.onCurioGetLooting(curio, entry, slotContext, source, target, stack, looting + entry.getLevel());
    }
}*/
